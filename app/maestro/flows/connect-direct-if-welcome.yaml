# NOTE: Render through the workspace-create shell scripts before running when
# using ${SOLO_MAESTRO_*}. Maestro does not substitute environment vars.
appId: ${SOLO_MAESTRO_APP_ID}
---
# Connect to the local daemon only when the app is on the welcome screen.

- runFlow:
    when:
      visible:
        id: "welcome-screen"
    commands:
      - tapOn:
          id: "welcome-direct-connection"

      - extendedWaitUntil:
          visible:
            id: "add-host-modal"
          timeout: 10000

      - tapOn:
          id: "direct-host-input"
      - eraseText
      - inputText: ${SOLO_MAESTRO_DIRECT_ENDPOINT}

      - assertVisible:
          id: "direct-host-input"
          text: ${SOLO_MAESTRO_DIRECT_ENDPOINT}

      - tapOn:
          id: "direct-host-submit"
