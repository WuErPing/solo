# Solo Relay environment configuration.
# Install to /opt/solo-relay/solo-relay.env on the relay host.

PORT=8080
HOST=127.0.0.1
MAX_BUFFER=200
MAX_CONNS=10000
LOG_LEVEL=info
# Comma-separated CORS whitelist for browser clients.
ALLOWED_ORIGINS=https://solo.up2ai.top
