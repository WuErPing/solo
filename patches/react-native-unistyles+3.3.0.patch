diff --git a/node_modules/react-native-unistyles/lib/commonjs/core/withUnistyles/withUnistyles.js b/node_modules/react-native-unistyles/lib/commonjs/core/withUnistyles/withUnistyles.js
index 5a0d049..7b1c1c2 100644
--- a/node_modules/react-native-unistyles/lib/commonjs/core/withUnistyles/withUnistyles.js
+++ b/node_modules/react-native-unistyles/lib/commonjs/core/withUnistyles/withUnistyles.js
@@ -33,8 +33,10 @@ const withUnistyles = (Component, mappings) => {
       ...unistyleProps
     } = narrowedProps.uniProps ? narrowedProps.uniProps(proxifiedTheme, proxifiedRuntime) : {};
     const emptyStyles = narrowedProps.style ? Object.fromEntries(Object.entries(Object.getOwnPropertyDescriptors(narrowedProps.style)).filter(([key]) => !key.startsWith('unistyles') && !key.startsWith('_')).map(([key]) => [key, undefined])) : undefined;
+    // Prevent `uniProps` from leaking onto wrapped components (and DOM elements on web)
+    const { uniProps: _ignoredUniProps, ...propsWithoutUniProps } = props;
     const combinedProps = {
-      ...(0, _utils.deepMergeObjects)(mappingsProps, unistyleProps, props),
+      ...(0, _utils.deepMergeObjects)(mappingsProps, unistyleProps, propsWithoutUniProps),
       ...(narrowedProps.style ? {
         // Override default component styles with undefined values to reset them
         style: emptyStyles
diff --git a/node_modules/react-native-unistyles/lib/module/core/withUnistyles/withUnistyles.js b/node_modules/react-native-unistyles/lib/module/core/withUnistyles/withUnistyles.js
index 5904a73..d52300a 100644
--- a/node_modules/react-native-unistyles/lib/module/core/withUnistyles/withUnistyles.js
+++ b/node_modules/react-native-unistyles/lib/module/core/withUnistyles/withUnistyles.js
@@ -28,8 +28,10 @@ export const withUnistyles = (Component, mappings) => {
       ...unistyleProps
     } = narrowedProps.uniProps ? narrowedProps.uniProps(proxifiedTheme, proxifiedRuntime) : {};
     const emptyStyles = narrowedProps.style ? Object.fromEntries(Object.entries(Object.getOwnPropertyDescriptors(narrowedProps.style)).filter(([key]) => !key.startsWith('unistyles') && !key.startsWith('_')).map(([key]) => [key, undefined])) : undefined;
+    // Prevent `uniProps` from leaking onto wrapped components (and DOM elements on web)
+    const { uniProps: _ignoredUniProps, ...propsWithoutUniProps } = props;
     const combinedProps = {
-      ...deepMergeObjects(mappingsProps, unistyleProps, props),
+      ...deepMergeObjects(mappingsProps, unistyleProps, propsWithoutUniProps),
       ...(narrowedProps.style ? {
         // Override default component styles with undefined values to reset them
         style: emptyStyles
diff --git a/node_modules/react-native-unistyles/src/core/withUnistyles/withUnistyles.tsx b/node_modules/react-native-unistyles/src/core/withUnistyles/withUnistyles.tsx
index e347b7a..cf36ff7 100644
--- a/node_modules/react-native-unistyles/src/core/withUnistyles/withUnistyles.tsx
+++ b/node_modules/react-native-unistyles/src/core/withUnistyles/withUnistyles.tsx
@@ -45,8 +45,11 @@ export const withUnistyles = <TComponent, TMappings extends GenericComponentProp
               )
             : undefined
 
+        // Prevent `uniProps` from leaking onto wrapped components (and DOM elements on web)
+        const { uniProps: _ignoredUniProps, ...propsWithoutUniProps } = props
+
         const combinedProps = {
-            ...deepMergeObjects(mappingsProps, unistyleProps, props),
+            ...deepMergeObjects(mappingsProps, unistyleProps, propsWithoutUniProps),
             ...(narrowedProps.style
                 ? {
                       // Override default component styles with undefined values to reset them
